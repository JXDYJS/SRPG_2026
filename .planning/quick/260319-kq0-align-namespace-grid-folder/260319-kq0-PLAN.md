# Quick Task 260319-kq0: Task: Align Namespace with Folder Structure for Grid Folder - Plan

**Created:** 2026-03-19
**Status:** Ready for execution
**Mode:** quick-full

---

## Goal
Ensure all C# scripts inside the `Assets/Script/GamePlay/Grid` folder use the exact namespace `GamePlay.Grid`.

---

## Must-Haves

**Truths:**
- All Grid folder files must use `namespace GamePlay.Grid`
- Unity compilation must pass after each file change
- Cross-folder references must be updated with correct `using` statements only

**Artifacts:**
- Updated namespace declarations in Grid folder files
- Updated `using` statements in dependent files (if needed)

**Key Links:**
- Target files: Astar.cs, MapInteraction.cs, LogicalGrid.cs, MapData.cs
- Already correct: AttackRangeSystem.cs, MapDataSO.cs

---

## Execution Plan

### Task 1: Update namespaces in Grid folder files (atomic, one-by-one)

**Files:**
- Assets/Script/GamePlay/Grid/Astar.cs
- Assets/Script/GamePlay/Grid/MapInteraction.cs
- Assets/Script/GamePlay/Grid/LogicalGrid.cs
- Assets/Script/GamePlay/Grid/MapData.cs

**Action:**
For each file in order:
1. Read the file and identify its current namespace declaration
2. Update the namespace to `GamePlay.Grid`
3. Save the file
4. Trigger Unity compilation using unity-debug skill
5. Check for compilation errors:
   - If no errors: Continue to next file
   - If errors in external files (CS0246, CS0234):
     - Open the external file
     - Update only the `using` statements (e.g., `using GamePlay;` → `using GamePlay.Grid;`)
     - Do not modify logic or variables
     - Save and recompile
   - If errors cannot be fixed by `using` statement updates: STOP and report

**Verify:**
- All 4 files have `namespace GamePlay.Grid`
- Unity compilation succeeds with zero errors
- Cross-folder references are resolved with correct `using` statements

**Done:**
- ✓ Astar.cs namespace changed from `GamePlay` to `GamePlay.Grid`
- ✓ MapInteraction.cs namespace changed from `MapSystem` to `GamePlay.Grid`
- ✓ LogicalGrid.cs namespace changed from `GamePlay` to `GamePlay.Grid`
- ✓ MapData.cs wrapped in `namespace GamePlay.Grid` (was no namespace)
- ✓ Unity compilation successful
- ✓ All cross-folder references resolved

---

## Notes
- AttackRangeSystem.cs and MapDataSO.cs already have correct namespace, skip them
- MapInteraction.cs has `namespace MapSystem` which will become `GamePlay.Grid`
- Astar.cs and LogicalGrid.cs currently use `namespace GamePlay`, will become `GamePlay.Grid`
- MapData.cs has no namespace, needs to be wrapped
